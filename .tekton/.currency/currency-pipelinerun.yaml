apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: currency-pipelinerun
spec:
  params:
  - name: revision
    value: "currency-update"
  pipelineRef:
    name: currency-pipeline
  serviceAccountName: currency-serviceaccount
  workspaces:
  - name: currency-pvc
    volumeClaimTemplate:
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 100Mi
